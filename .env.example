# Debug builds only. Copy this file to .env and fill in the values you need.
# Release builds ignore these variables except SIMPLE_STT_AI_API_KEY when it is
# explicitly supplied by the launching environment.
SIMPLE_STT_AI_API_KEY=
SIMPLE_STT_AI_BASE_URL=
SIMPLE_STT_AI_MODEL=

# Third-party notices

Simple STT includes and/or is packaged with third-party components. Their licenses remain with their respective owners.

## Packaged runtime components

- AutoHotkey v2 runtime, used for the desktop shell executable.
- Parakeet runtime files and speech model files under `runtime/external/parakeet-runtime/`, when included in a packaged build.
- Rust crate dependencies compiled into the Simple STT binaries, as recorded in `Cargo.lock`.

See the upstream projects and bundled files for detailed license terms.
